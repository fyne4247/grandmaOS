[Resolve]
# NetworkManager's dnsmasq plugin listens here on Mint. It applies the narrow
# OpenRouter exception before forwarding every other name to AdGuard Family.
DNS=127.0.1.1
FallbackDNS=
Domains=~.
